spring:
  application:
    name: quicklib
  datasource:
    url: jdbc:postgresql://quicklib-db:5432/quicklib

jpa:
  hibernate:
    ddl-auto: update
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect

frontend:
  url: ${FRONTEND_URL}
